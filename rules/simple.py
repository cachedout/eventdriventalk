# -*- encoding: utf-8 -*-

'''
Simple rules

All rules must return simply True or False
'''

def gt(value, thresh):
    return value > thresh

def lt(value, thresh):
    return value < thresh

def eq(value, thresh):
    return value == thresh

